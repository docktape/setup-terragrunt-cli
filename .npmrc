; Require any installed package version to be at least 30 days old.
; Defends against fast-pulled supply-chain attacks (a compromised version
; published and unpublished within hours never satisfies this threshold).
; Value is in DAYS. Requires npm CLI >= 11.10.0.
min-release-age=30
